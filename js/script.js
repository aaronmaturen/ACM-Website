/* Author: Aaron T. Maturen <atmature@svsu.edu>

*/



$(function(){
	// Bind the hashchange event.
	// This needs to be used to update the homepage on the site
	
	//$(window).hashchange( function(){
		//strip the '#' from the beginning of the hash		
		//var hashValue = window.location.hash.slice(1);
		//$('#ajaxHolder').html(hashValue);
	//});
	
	//trigger the hashChange in order to support bookmarked sites
	//$(window).hashchange();

});

